name: customer-support
description: AI customer support ticket triage and resolution environment
version: 1.0

entrypoint: env.environment:CustomerSupportEnv

tasks:
  - easy
  - medium
  - hard

observation:
  ticket: string
  priority: string
  sentiment: string
  sla_hours: float
  step: integer
  instructions: string

action:
  response: string

reward:
  type: float